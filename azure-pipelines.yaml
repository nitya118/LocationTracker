trigger:
  branches:
    include:
      - infra_*
  paths:
    include:
      -  'terraform/*'
      -  'api/*'
  
# infra pipleine
steps:
- task: PublishPipelineArtifact@1
  inputs:
    targetPath: '$(System.DefaultWorkingDirectory)/'
    artifact: 'infra'
    publishLocation: 'pipeline'